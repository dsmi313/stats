# Section 6 — Stratification: when pooling lies

# Goals:
# - Simulate parameter shifts across strata
# - Show bias from pooled estimation
# - Estimate strata separately and compare

library(ggplot2)
library(dplyr)

set.seed(2026)

# TODO: simulate a season with changing p_n and compare pooled vs stratified estimates
# TODO: visualize CI widths and coverage for different stratum sizes
